<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             version="2.2"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd">

    <!--
      Plantilla de configuración JPA (no usada por el código actual con Javalin + JDBC).
      Puede servirte en el futuro si decides migrar a Hibernate/JPA.

      Notas:
      - No olvides agregar las dependencias de JPA/Hibernate en el pom.xml si vas a usarlo.
      - Este archivo por sí solo NO habilita JPA; el proyecto actual usa JDBC con HikariCP.
    -->
    <persistence-unit name="deliveryPU" transaction-type="RESOURCE_LOCAL">
        <!-- Proveedor JPA sugerido -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Mapea aquí tus entidades si usas classpath scanning -->
        <!-- <class>com.mycompany.delivery.api.model.Usuario</class> -->

        <properties>
            <!-- Datos de conexión -->
            <property name="javax.persistence.jdbc.driver" value="org.postgresql.Driver"/>
            <property name="javax.persistence.jdbc.url" value="jdbc:postgresql://localhost:5432/postgres"/>
            <property name="javax.persistence.jdbc.user" value="postgres"/>
            <property name="javax.persistence.jdbc.password" value=""/>

            <!-- Dialect y opciones de Hibernate -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>
            <property name="hibernate.hbm2ddl.auto" value="none"/>
            <property name="hibernate.show_sql" value="false"/>
            <property name="hibernate.format_sql" value="false"/>
        </properties>
    </persistence-unit>
</persistence>
